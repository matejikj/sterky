---
import Layout from '../../layouts/Layout.astro';
import PillButton from '../../components/ui/PillButton.astro';
import Section from '../../components/ui/Section.astro';
import SurfacePanel from '../../components/ui/SurfacePanel.astro';
import { sterkyTypes } from '../../data/sterky';

export const prerender = true;
---

<Layout title="Sterky | Typy povrchu" description="Placeholder listing typu sterek.">
  <Section>
    <div class="grid gap-[14px]">
      <SurfacePanel class="showroom-light p-[16px] md:p-[20px]">
        <p class="eyebrow">Collections / material types</p>
        <h1 class="mt-[10px]">Typy sterek</h1>
        <p class="mt-[14px] max-w-[72ch] text-[16px] leading-[1.7] text-charcoal-900/80">
          Listing route je pripraveny pro SEO content a pozdejsi switcher kolekci. Zde se budou prolinat technicke parametry, finish swatche a prolinkovani do realizaci.
        </p>
        <div class="mt-[16px] flex flex-wrap gap-[8px]">
          <PillButton href="/realizace/" variant="outline">Projit realizace</PillButton>
          <PillButton href="/#poptavka" variant="ghost">Konzultace</PillButton>
        </div>
      </SurfacePanel>

      <div class="grid gap-[14px] md:grid-cols-3">
        {sterkyTypes.map((item, index) => (
          <SurfacePanel as="article" tone={index === 0 ? 'sand' : 'light'} class="p-[16px] md:p-[20px]">
            <p class="spec-label">Type {String(index + 1).padStart(2, '0')}</p>
            <h3 class="mt-[10px]">{item.title}</h3>
            <p class="mt-[10px] text-[14px] leading-[1.6] text-charcoal-900/80">{item.summary}</p>
            <a
              href={`/sterky/${item.slug}/`}
              class="mt-[12px] inline-flex min-h-[44px] items-center rounded-[100px] border border-black/10 px-[14px] font-mono text-[12px] tracking-[0.08em] uppercase hover:bg-black/5"
            >
              Detail /sterky/{item.slug}/
            </a>
          </SurfacePanel>
        ))}
      </div>
    </div>
  </Section>
</Layout>
