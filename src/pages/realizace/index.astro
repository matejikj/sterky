---
import Layout from '../../layouts/Layout.astro';

export const prerender = true;
---

<Layout title="Realizace | Galerie projektu" description="Placeholder listing realizaci sterek.">
  <section class="mx-auto w-full max-w-7xl px-3 py-16 md:px-[18px] md:py-24">
    <h1 class="text-4xl leading-tight font-medium md:text-5xl">Realizace</h1>
    <p class="mt-4 max-w-2xl text-base leading-7 text-neutral-700">
      Placeholder galerie realizaci. V dalsich taskach pribude semanticka galerie, detail projektu, close-up textury a technicka data o systemu.
    </p>
    <div class="mt-10 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
      {Array.from({ length: 6 }).map((_, i) => (
        <article class="rounded-xl border border-black/8 bg-white p-4">
          <div class="aspect-[4/3] rounded-lg border border-black/6 bg-stone-200"></div>
          <h2 class="mt-3 text-base font-medium">Projekt {i + 1}</h2>
          <p class="mt-1 text-sm text-neutral-700">Placeholder pro detail realizace a pouzity system vrstev.</p>
        </article>
      ))}
    </div>
  </section>
</Layout>
