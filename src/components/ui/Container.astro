---
interface Props {
  as?: keyof HTMLElementTagNameMap;
  class?: string;
}

const { as: Tag = 'div', class: className } = Astro.props;
---

<Tag class:list={['mx-auto w-full max-w-[144rem] px-[12px] md:px-[18px]', className]}>
  <slot />
</Tag>

